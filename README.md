# Kodi Addon Builder

A CLI tool to automate version bumping, committing, tagging, pushing, releasing, and local zip artifact generation for Kodi addons.

## Installation

Install from git:

```bash
pip install git+https://github.com/yourusername/kodi-addon-builder.git
```

## Usage

```bash
kodi-addon-builder --help
```